'use strict';

const models = require('./models');
const api = require('./api');
const config = require('./config');
const core = require('./core');
const plugins = require('./plugins');

module.exports = { models, api, config, core, plugins };
