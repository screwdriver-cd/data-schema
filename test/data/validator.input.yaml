yaml: |
  jobs:
      main:
          image: node:{{NODE_VERSION}}
          matrix:
              NODE_VERSION: [4,5,6]
          steps:
              install: npm install
              test: npm test
              build: npm run build
          settings:
              email: foo@bar.com

      publish:
          image: node:4
          steps:
              install: npm publish
