name: test/foo/bar
namespace: template
version: "1.3"
description: Pipeline template for testing
maintainer: foo@bar.com
config:
    jobs:
        main:
            steps:
                -   init: npm install
                -   test: npm test

