canary:
    jobs:
        - first
        - second
    description: "Canary jobs for testing"
    sourcePaths:
        - src/
prod:
    jobs:
        - deploy-west
        - deploy-east
    description: "Prod deploy jobs"
